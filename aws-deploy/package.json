{
  "name": "aws-deploy",
  "version": "0.1.0",
  "bin": {
    "aws-deploy": "bin/aws-deploy.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.178.2",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.178.2",
    "constructs": "^10.0.0"
  }
}
